package com.colingodsey.logos.collections

/**
 * Mutable vector that produces an immutable vector. Preferrably single alloc.
 * TODO: use special macro function to optimize block content to use builder
 * instead of strings if immutable object ops. Could also be available via
 * compiler plugin.
 */
class VecOpBuilder {

}
